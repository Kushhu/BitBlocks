import { BitTextboxDirective } from './bit-textbox.directive';

describe('BitTextboxDirective', () => {
  it('should create an instance', () => {
    const directive = new BitTextboxDirective();
    expect(directive).toBeTruthy();
  });
});
