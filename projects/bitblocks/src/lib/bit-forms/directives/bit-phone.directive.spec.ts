import { BitPhoneDirective } from './bit-phone.directive';

describe('BitPhoneDirective', () => {
  it('should create an instance', () => {
    const directive = new BitPhoneDirective();
    expect(directive).toBeTruthy();
  });
});
