import { BitNumberboxDirective } from './bit-numberbox.directive';

describe('BitNumberboxDirective', () => {
  it('should create an instance', () => {
    const directive = new BitNumberboxDirective();
    expect(directive).toBeTruthy();
  });
});
