import { BitEmailboxDirective } from './bit-emailbox.directive';

describe('BitEmailboxDirective', () => {
  it('should create an instance', () => {
    const directive = new BitEmailboxDirective();
    expect(directive).toBeTruthy();
  });
});
